name = "status-page"
compatibility_date = "2026-07-07"
pages_build_output_dir = "frontend/dist"

# Optional but recommended for Cloudflare Pages:
# bind a Workers KV namespace named STATUS_CACHE so /api/status can read the
# last successful backend snapshot across browser sessions and edge instances.
# [[kv_namespaces]]
# binding = "STATUS_CACHE"
# id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
